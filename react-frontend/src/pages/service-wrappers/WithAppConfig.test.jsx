
describe('WithAppConfig', () => {
  xit('should show the PageLoader.', function() {});
  describe('after the application config has loaded', () => {
    xit('should render the children with the config.', () => {});
    xit('should hide the PageLoader.', () => {});
  });
  describe('after failing to load the application config', function() {
    xit('should allow the network error to bubble up.', () => {});
    xit('should hide the PageLoader.', () => {});
  });
});
