table:
  schema: app
  name: deal_event_type
array_relationships:
  - name: deals
    using:
      foreign_key_constraint_on:
        column: event_type
        table:
          schema: app
          name: deal
