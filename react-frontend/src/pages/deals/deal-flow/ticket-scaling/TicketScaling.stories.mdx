import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import { TICKET_TYPES } from '~/test';
import { generate } from '~/services/mocks';
import { TicketScaling } from './TicketScaling.jsx';

<Meta title="Pages/DealFlow/TicketScaling" />

# TicketScaling

<Description of={ TicketScaling } />

<Canvas>
  <Story name="TicketScaling">
    <TicketScaling
      deal={generate.app_deal({allowEmpty: true})}
      config={{TICKET_TYPES}}
    />
  </Story>
</Canvas>

<ArgsTable of={ TicketScaling } />
