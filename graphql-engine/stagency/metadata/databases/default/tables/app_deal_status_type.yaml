table:
  schema: app
  name: deal_status_type
is_enum: true
array_relationships:
  - name: deals
    using:
      foreign_key_constraint_on:
        column: status_type
        table:
          schema: app
          name: deal
