{
  // CoC.nvim wants to treat this as the project workspace and as
  // a result does not report Typescript errors. Adding the tsconfg
  // at this level helps fix the issue but I need to diagnose how
  // to properly setup this project for IDEs.
  // https://github.com/neoclide/coc.nvim/wiki/Debug-language-server#using-output-channel
  "extends": "./react-frontend/tsconfig",
  "include": [
    "./react-frontend/src/**/*"
  ]
}
