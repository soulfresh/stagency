import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import { {{pascalCase name}} } from './{{pascalCase name}}';

<Meta title="{{pascalCase componentType}}s/{{pascalCase name}}" />

# {{pascalCase name}}

<Description of={ {{pascalCase name}} } />

<Canvas>
  <Story name="{{pascalCase name}}">
    <{{pascalCase name}} />
  </Story>
</Canvas>

<ArgsTable of={ {{pascalCase name}} } />
