
describe('TimeInputCell', () => {
  describe('with a time value', () => {
    xit('should display the time value it is given.', () => {});
    xit('should emit the updated time when it is changed.', () => {});
  });
  describe('with a duration', () => {
    xit('should display the duration value it is given.', () => {});
    xit('should emit the updated duration value when it changes.', () => {});
  });
})
