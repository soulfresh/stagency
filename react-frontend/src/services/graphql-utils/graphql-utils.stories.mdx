import { Meta, ArgsTable, Description } from '@storybook/addon-docs'

import API from './index.md'

<Meta title="Services/GraphQL Utils/API" />

# GraphQL Utilities

The utilities in this package are intended to be used across all GraphQL
end points. They allow us to integrate those endpoints with our app in
a consistent manner that ties into the global logging and authentication
systems.

## API

<Description>{API}</Description>
