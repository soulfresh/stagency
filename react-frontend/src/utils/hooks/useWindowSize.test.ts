// import { useWindowSize } from './useWindowSize';

// TEMP required to pass linting. Remove once the test is implemented.
export {}

describe('useWindowSize', () => {
  xit('should be able to determine the current window size.', () => {});
})
