describe('Main', () => {
  xit('should request the application config on mount.', () => {});
  describe('after an error occurs loading the application config', () => {
    xit('should show the global error page.', () => {});
  })
})
